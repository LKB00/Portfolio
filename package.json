{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "description": "Local dev tooling only (dev-server.js). No build step, no dependencies — the static site itself needs none of this, and Vercel deploys it unchanged."
}
